// Template file for SubWCRev to hold version control information
var RBTDEV_VersionInfo = {
		Revision : "$WCREV$",
		Modified : "$WCMODS?Modified:Not modified$",
		LastRevDate: "$WCDATE$",
		BuildDate: "$WCNOW$",
		Range    : "$WCRANGE$",
		Mixed    : "$WCMIXED?Mixed revision WC:Not mixed$",
		URL      : "$WCURL$"
	}
